import RecruiterReducer from '../recruiter';

describe('Test Recruiter Reducer', () => {
  it('Test ... should return ... correctly.', (done) => {
    // const match = { id: 1, name: 'SIT Career Day' };
    // const action = { type: 'leo/match/set/match', match };

    // const store = RecruiterReducer({}, action);

    // expect(store).toEqual({ match });
    // done();
    expect(true).toEqual(true);
    done();
  });
});
